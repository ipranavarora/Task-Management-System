export interface WishItem {
  id: string; // Add id property
  wishText: string;
  description: string;
  dueDate: string;
  priority: string;
  status: string;
  isComplete: boolean; // Add isComplete property
}